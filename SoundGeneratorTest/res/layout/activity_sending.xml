<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SendingActivity" >
	
    <EditText
		    android:id="@+id/text_to_send"
		    android:layout_width="match_parent"
		    android:layout_height="wrap_content"
		    android:hint="@string/string_values"
		    android:inputType="text"
		    android:imeActionLabel="@string/launch" />

    <Button
        android:id="@+id/start_to_play"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/text_to_send"
        android:layout_below="@+id/text_to_send"
        android:layout_marginTop="49dp"
        android:text="Start play" />

        <TextView
            android:id="@+id/playing_txt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignLeft="@+id/start_to_play"
            android:layout_alignRight="@+id/start_to_play"
            android:layout_below="@+id/start_to_play"
            android:layout_marginTop="26dp" />

</RelativeLayout>
